global:
  scrape_interval: 15s # How often to check the system
scrape_configs:
  - job_name: 'meetmux-api'
    static_configs:
      - targets: ['host.docker.internal:3000'] # Points to your Node.js app